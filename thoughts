antialiasing

t_rt:
	mlx		*mlx;
	t_scene	*scene;

t_scene:
	t_obj	*objs_arr;
	t_cam	*cam;
	t_light *lights_arr;

t_obj:
	int	type;
	int	*attrs;

#define SPHERE_RADIUS_ATTR_INDEX 1

sphere.attrs[SPHERE_RADIUS_ATTR_INDEX] 

DEFINES:
	if type == sphere
		attr_len = 3;
		attrs[0] = radius;
		attrs[1] = diametr

plane 
sphere
cylinder

